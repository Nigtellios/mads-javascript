{
  "name": "MADS-JavaScript",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "build-src": "parcel build src/*",
    "dev": "parcel index.html --open",
    "dev-src": "parcel src/index.html --open",
    "start": "npm run build && npm run dev",
    "start-src": "npm run build-src && npm run dev-src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.35.2"
  },
  "dependencies": {
    "@popperjs/core": "^2.9.2",
    "bootstrap": "^5.0.2",
    "yarn": "^1.22.10"
  }
}
